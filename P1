def Fact(x):
    if x > 1:
        return (x - 1) * Fact(x - 1)

    else:
        return 1


Num = int(input("Enter a number to get its factorial"))
print("The factorial of " + str(Num) + " is " + str(Num * Fact(Num)))
